/**
 * Created by vulong on 06/03/2020.
 */

let socket_definer = {
    ROOM_ID : 'ROOM_AMZ_CRAWL_ORDER'
};

let event = {
    JOIN_ROOM : 'JOIN_ROOM',
    JOINED_SUCCESS : 'JOINED_SUCCESS'
};


module.exports = { socket_definer: socket_definer, event: event};
